// AUTO-GENERATED by generator.ts — do NOT edit by hand.
// Run `npm run generate-decoders` after changing generator.ts or after CSL
// adds/removes decodable types. Generator invariant: get_decodable_types,
// decode_specific_type, and get_possible_types_for_input are all views over
// the same registry, so the three stay in sync by construction.


use std::collections::HashMap;
use std::sync::OnceLock;

use crate::bingen::wasm_bindgen;
use crate::csl_decoders::params::DecodingParams;
use crate::csl_decoders::specific_decoders::{
    decode_address, decode_native_script, decode_plutus_data, decode_plutus_script,
    decode_transaction,
};
use crate::js_value::{from_js_value, from_serde_json_value, JsValue};
use bech32;
use bs58;
use cardano_serialization_lib as csl;
use hex;

fn is_valid_hex(input: &str) -> bool {
    hex::decode(input).is_ok()
}

fn is_valid_base58(input: &str) -> bool {
    bs58::decode(input).into_vec().is_ok()
}

fn is_valid_bech32(input: &str) -> bool {
    bech32::decode(input).is_ok()
}

/// Signature every decoder in the registry must satisfy.
type DecoderFn =
    fn(&str, bool, bool, bool, &DecodingParams) -> Result<JsValue, String>;



fn decode_address_shim(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    decode_address(input, is_hex, is_bech32, is_base58)
}

fn decode_transaction_shim(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    decode_transaction(input, is_hex, is_bech32, is_base58)
}

fn decode_native_script_shim(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    decode_native_script(input, is_hex, is_bech32, is_base58)
}

fn decode_plutus_data_shim(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    params: &DecodingParams,
) -> Result<JsValue, String> {
    decode_plutus_data(input, params.plutus_data_schema.clone(), is_hex, is_bech32, is_base58)
}

fn decode_plutus_script_shim(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    params: &DecodingParams,
) -> Result<JsValue, String> {
    decode_plutus_script(input, params.plutus_script_version, is_hex, is_bech32, is_base58)
}

fn decode_anchor(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Anchor::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_anchor_data_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::AnchorDataHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::AnchorDataHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_asset_name(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::AssetName::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_asset_names(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::AssetNames::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_assets(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Assets::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_auxiliary_data(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::AuxiliaryData::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_auxiliary_data_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::AuxiliaryDataHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::AuxiliaryDataHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_big_int(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::BigInt::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_big_num(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::BigNum::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_bip32_private_key(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Bip32PrivateKey::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::Bip32PrivateKey::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_bip32_public_key(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Bip32PublicKey::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::Bip32PublicKey::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_block(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Block::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_block_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::BlockHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::BlockHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_bootstrap_witness(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::BootstrapWitness::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_bootstrap_witnesses(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::BootstrapWitnesses::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_certificate(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Certificate::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_certificates(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Certificates::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_committee(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Committee::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_committee_cold_resign(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::CommitteeColdResign::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_committee_hot_auth(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::CommitteeHotAuth::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_constitution(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Constitution::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_constr_plutus_data(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ConstrPlutusData::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_costmdls(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Costmdls::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_cost_model(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::CostModel::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_credential(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Credential::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_credentials(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Credentials::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_data_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::DataHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::DataHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_dns_record_aor_aaaa(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::DNSRecordAorAAAA::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_dns_record_srv(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::DNSRecordSRV::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_d_rep(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::DRep::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::DRep::from_bech32(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_d_rep_deregistration(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::DRepDeregistration::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_d_rep_registration(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::DRepRegistration::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_d_rep_update(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::DRepUpdate::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_d_rep_voting_thresholds(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::DRepVotingThresholds::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_ed25519_key_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Ed25519KeyHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::Ed25519KeyHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_ed25519_key_hashes(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Ed25519KeyHashes::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_ed25519_signature(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Ed25519Signature::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::Ed25519Signature::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_ex_unit_prices(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ExUnitPrices::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_ex_units(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ExUnits::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_general_transaction_metadata(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::GeneralTransactionMetadata::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_genesis_delegate_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::GenesisDelegateHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::GenesisDelegateHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_genesis_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::GenesisHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::GenesisHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_genesis_hashes(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::GenesisHashes::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_genesis_key_delegation(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::GenesisKeyDelegation::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_governance_action(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::GovernanceAction::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_governance_action_id(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::GovernanceActionId::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_hard_fork_initiation_action(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::HardForkInitiationAction::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_header(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Header::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_header_body(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::HeaderBody::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_int(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Int::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_ipv4(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Ipv4::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_ipv6(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Ipv6::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_kes_signature(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(bytes) = hex::decode(input) {
            if let Ok(decoded) = csl::KESSignature::from_bytes(bytes) {
                let value = {
                let _ = &decoded;
                Ok::<serde_json::Value, String>(serde_json::Value::String(
                    "Decoded, but no additional representation".to_string(),
                ))
            }?;
                return from_serde_json_value(&value)
                    .map_err(|e| format!("Failed to convert to JsValue: {}", e));
            }
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_kesv_key(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::KESVKey::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::KESVKey::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_language(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Language::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_legacy_daedalus_private_key(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(bytes) = hex::decode(input) {
            if let Ok(decoded) = csl::LegacyDaedalusPrivateKey::from_bytes(&bytes) {
                let value = {
                let _ = &decoded;
                Ok::<serde_json::Value, String>(serde_json::Value::String(
                    "Decoded, but no additional representation".to_string(),
                ))
            }?;
                return from_serde_json_value(&value)
                    .map_err(|e| format!("Failed to convert to JsValue: {}", e));
            }
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_metadata_list(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::MetadataList::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_metadata_map(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::MetadataMap::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_mint(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Mint::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_mir_to_stake_credentials(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::MIRToStakeCredentials::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_move_instantaneous_reward(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::MoveInstantaneousReward::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_move_instantaneous_rewards_cert(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::MoveInstantaneousRewardsCert::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_multi_asset(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::MultiAsset::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_multi_host_name(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::MultiHostName::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_native_scripts(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::NativeScripts::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_network_id(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::NetworkId::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_new_constitution_action(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::NewConstitutionAction::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_no_confidence_action(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::NoConfidenceAction::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_nonce(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Nonce::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_operational_cert(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::OperationalCert::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_parameter_change_action(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ParameterChangeAction::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_plutus_list(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PlutusList::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_plutus_map(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PlutusMap::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_plutus_scripts(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PlutusScripts::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_pool_metadata(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PoolMetadata::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_pool_metadata_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PoolMetadataHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::PoolMetadataHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_pool_params(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PoolParams::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_pool_registration(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PoolRegistration::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_pool_retirement(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PoolRetirement::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_pool_voting_thresholds(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PoolVotingThresholds::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_private_key(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PrivateKey::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::PrivateKey::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_proposed_protocol_parameter_updates(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ProposedProtocolParameterUpdates::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_protocol_param_update(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ProtocolParamUpdate::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_protocol_version(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ProtocolVersion::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_public_key(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::PublicKey::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::PublicKey::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded.to_bech32()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_redeemer(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Redeemer::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_redeemers(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Redeemers::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_redeemer_tag(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::RedeemerTag::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_relay(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Relay::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_relays(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Relays::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_reward_addresses(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::RewardAddresses::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_script_all(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ScriptAll::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_script_any(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ScriptAny::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_script_data_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ScriptDataHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::ScriptDataHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_script_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ScriptHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::ScriptHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_script_hashes(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ScriptHashes::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_script_n_of_k(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ScriptNOfK::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_script_pubkey(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ScriptPubkey::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_script_ref(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::ScriptRef::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_single_host_addr(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::SingleHostAddr::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_single_host_name(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::SingleHostName::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_stake_and_vote_delegation(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::StakeAndVoteDelegation::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_stake_delegation(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::StakeDelegation::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_stake_deregistration(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::StakeDeregistration::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_stake_registration(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::StakeRegistration::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_stake_registration_and_delegation(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::StakeRegistrationAndDelegation::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_stake_vote_registration_and_delegation(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::StakeVoteRegistrationAndDelegation::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_timelock_expiry(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TimelockExpiry::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_timelock_start(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TimelockStart::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_bodies(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionBodies::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_body(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionBody::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::TransactionHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_input(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionInput::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_inputs(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionInputs::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_metadatum(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionMetadatum::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_metadatum_labels(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionMetadatumLabels::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex()
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_output(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionOutput::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_outputs(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionOutputs::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_unspent_output(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionUnspentOutput::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_witness_set(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionWitnessSet::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_transaction_witness_sets(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TransactionWitnessSets::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_treasury_withdrawals_action(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::TreasuryWithdrawalsAction::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_unit_interval(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::UnitInterval::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_update(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Update::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_update_committee_action(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::UpdateCommitteeAction::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_url(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::URL::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_value(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Value::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_versioned_block(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VersionedBlock::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_vkey(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Vkey::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_vkeywitness(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Vkeywitness::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_vkeywitnesses(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Vkeywitnesses::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_vote_delegation(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VoteDelegation::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_voter(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Voter::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_vote_registration_and_delegation(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VoteRegistrationAndDelegation::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_voting_procedure(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VotingProcedure::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_voting_procedures(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VotingProcedures::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_voting_proposal(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VotingProposal::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_voting_proposals(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VotingProposals::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_vrf_cert(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VRFCert::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_vrf_key_hash(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VRFKeyHash::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::VRFKeyHash::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_vrfv_key(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::VRFVKey::from_hex(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    if is_bech32 {
        if let Ok(decoded) = csl::VRFVKey::from_bech32(input) {
            let value = Ok::<serde_json::Value, String>(serde_json::json!({
                "hex": decoded.to_hex(),
                "bech32": decoded
                    .to_bech32("")
                    .map_err(|e| format!("Failed to convert to bech32: {:?}", e))?
            }))?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

fn decode_withdrawals(
    input: &str,
    is_hex: bool,
    is_bech32: bool,
    is_base58: bool,
    _params: &DecodingParams,
) -> Result<JsValue, String> {
    let _ = (is_hex, is_bech32, is_base58);
    if is_hex {
        if let Ok(decoded) = csl::Withdrawals::from_hex(input) {
            let value = decoded
                .to_json()
                .map_err(|e| format!("Failed to convert to JSON: {:?}", e))
                .and_then(|json| {
                    serde_json::from_str(&json)
                        .map_err(|e| format!("Failed to parse JSON: {}", e))
                })?;
            return from_serde_json_value(&value)
                .map_err(|e| format!("Failed to convert to JsValue: {}", e));
        }
    }

    Err("Failed to decode".to_string())
}

/// Registry of every type the universal decoder can attempt. Populated once
/// on first call via `OnceLock`. Iteration order is unspecified — callers
/// that need a stable list (e.g. `get_decodable_types`) sort explicitly.
fn decoders() -> &'static HashMap<&'static str, DecoderFn> {
    static DECODERS: OnceLock<HashMap<&'static str, DecoderFn>> = OnceLock::new();
    DECODERS.get_or_init(|| {
        let mut m: HashMap<&'static str, DecoderFn> = HashMap::with_capacity(152);
        m.insert("Address", decode_address_shim as DecoderFn);
        m.insert("Anchor", decode_anchor as DecoderFn);
        m.insert("AnchorDataHash", decode_anchor_data_hash as DecoderFn);
        m.insert("AssetName", decode_asset_name as DecoderFn);
        m.insert("AssetNames", decode_asset_names as DecoderFn);
        m.insert("Assets", decode_assets as DecoderFn);
        m.insert("AuxiliaryData", decode_auxiliary_data as DecoderFn);
        m.insert("AuxiliaryDataHash", decode_auxiliary_data_hash as DecoderFn);
        m.insert("BaseAddress", decode_address_shim as DecoderFn);
        m.insert("BigInt", decode_big_int as DecoderFn);
        m.insert("BigNum", decode_big_num as DecoderFn);
        m.insert("Bip32PrivateKey", decode_bip32_private_key as DecoderFn);
        m.insert("Bip32PublicKey", decode_bip32_public_key as DecoderFn);
        m.insert("Block", decode_block as DecoderFn);
        m.insert("BlockHash", decode_block_hash as DecoderFn);
        m.insert("BootstrapWitness", decode_bootstrap_witness as DecoderFn);
        m.insert("BootstrapWitnesses", decode_bootstrap_witnesses as DecoderFn);
        m.insert("ByronAddress", decode_address_shim as DecoderFn);
        m.insert("Certificate", decode_certificate as DecoderFn);
        m.insert("Certificates", decode_certificates as DecoderFn);
        m.insert("Committee", decode_committee as DecoderFn);
        m.insert("CommitteeColdResign", decode_committee_cold_resign as DecoderFn);
        m.insert("CommitteeHotAuth", decode_committee_hot_auth as DecoderFn);
        m.insert("Constitution", decode_constitution as DecoderFn);
        m.insert("ConstrPlutusData", decode_constr_plutus_data as DecoderFn);
        m.insert("Costmdls", decode_costmdls as DecoderFn);
        m.insert("CostModel", decode_cost_model as DecoderFn);
        m.insert("Credential", decode_credential as DecoderFn);
        m.insert("Credentials", decode_credentials as DecoderFn);
        m.insert("DataHash", decode_data_hash as DecoderFn);
        m.insert("DNSRecordAorAAAA", decode_dns_record_aor_aaaa as DecoderFn);
        m.insert("DNSRecordSRV", decode_dns_record_srv as DecoderFn);
        m.insert("DRep", decode_d_rep as DecoderFn);
        m.insert("DRepDeregistration", decode_d_rep_deregistration as DecoderFn);
        m.insert("DRepRegistration", decode_d_rep_registration as DecoderFn);
        m.insert("DRepUpdate", decode_d_rep_update as DecoderFn);
        m.insert("DRepVotingThresholds", decode_d_rep_voting_thresholds as DecoderFn);
        m.insert("Ed25519KeyHash", decode_ed25519_key_hash as DecoderFn);
        m.insert("Ed25519KeyHashes", decode_ed25519_key_hashes as DecoderFn);
        m.insert("Ed25519Signature", decode_ed25519_signature as DecoderFn);
        m.insert("EnterpriseAddress", decode_address_shim as DecoderFn);
        m.insert("ExUnitPrices", decode_ex_unit_prices as DecoderFn);
        m.insert("ExUnits", decode_ex_units as DecoderFn);
        m.insert("GeneralTransactionMetadata", decode_general_transaction_metadata as DecoderFn);
        m.insert("GenesisDelegateHash", decode_genesis_delegate_hash as DecoderFn);
        m.insert("GenesisHash", decode_genesis_hash as DecoderFn);
        m.insert("GenesisHashes", decode_genesis_hashes as DecoderFn);
        m.insert("GenesisKeyDelegation", decode_genesis_key_delegation as DecoderFn);
        m.insert("GovernanceAction", decode_governance_action as DecoderFn);
        m.insert("GovernanceActionId", decode_governance_action_id as DecoderFn);
        m.insert("HardForkInitiationAction", decode_hard_fork_initiation_action as DecoderFn);
        m.insert("Header", decode_header as DecoderFn);
        m.insert("HeaderBody", decode_header_body as DecoderFn);
        m.insert("Int", decode_int as DecoderFn);
        m.insert("Ipv4", decode_ipv4 as DecoderFn);
        m.insert("Ipv6", decode_ipv6 as DecoderFn);
        m.insert("KESSignature", decode_kes_signature as DecoderFn);
        m.insert("KESVKey", decode_kesv_key as DecoderFn);
        m.insert("Language", decode_language as DecoderFn);
        m.insert("LegacyDaedalusPrivateKey", decode_legacy_daedalus_private_key as DecoderFn);
        m.insert("MetadataList", decode_metadata_list as DecoderFn);
        m.insert("MetadataMap", decode_metadata_map as DecoderFn);
        m.insert("Mint", decode_mint as DecoderFn);
        m.insert("MIRToStakeCredentials", decode_mir_to_stake_credentials as DecoderFn);
        m.insert("MoveInstantaneousReward", decode_move_instantaneous_reward as DecoderFn);
        m.insert("MoveInstantaneousRewardsCert", decode_move_instantaneous_rewards_cert as DecoderFn);
        m.insert("MultiAsset", decode_multi_asset as DecoderFn);
        m.insert("MultiHostName", decode_multi_host_name as DecoderFn);
        m.insert("NativeScript", decode_native_script_shim as DecoderFn);
        m.insert("NativeScripts", decode_native_scripts as DecoderFn);
        m.insert("NetworkId", decode_network_id as DecoderFn);
        m.insert("NewConstitutionAction", decode_new_constitution_action as DecoderFn);
        m.insert("NoConfidenceAction", decode_no_confidence_action as DecoderFn);
        m.insert("Nonce", decode_nonce as DecoderFn);
        m.insert("OperationalCert", decode_operational_cert as DecoderFn);
        m.insert("ParameterChangeAction", decode_parameter_change_action as DecoderFn);
        m.insert("PlutusData", decode_plutus_data_shim as DecoderFn);
        m.insert("PlutusList", decode_plutus_list as DecoderFn);
        m.insert("PlutusMap", decode_plutus_map as DecoderFn);
        m.insert("PlutusScript", decode_plutus_script_shim as DecoderFn);
        m.insert("PlutusScripts", decode_plutus_scripts as DecoderFn);
        m.insert("PointerAddress", decode_address_shim as DecoderFn);
        m.insert("PoolMetadata", decode_pool_metadata as DecoderFn);
        m.insert("PoolMetadataHash", decode_pool_metadata_hash as DecoderFn);
        m.insert("PoolParams", decode_pool_params as DecoderFn);
        m.insert("PoolRegistration", decode_pool_registration as DecoderFn);
        m.insert("PoolRetirement", decode_pool_retirement as DecoderFn);
        m.insert("PoolVotingThresholds", decode_pool_voting_thresholds as DecoderFn);
        m.insert("PrivateKey", decode_private_key as DecoderFn);
        m.insert("ProposedProtocolParameterUpdates", decode_proposed_protocol_parameter_updates as DecoderFn);
        m.insert("ProtocolParamUpdate", decode_protocol_param_update as DecoderFn);
        m.insert("ProtocolVersion", decode_protocol_version as DecoderFn);
        m.insert("PublicKey", decode_public_key as DecoderFn);
        m.insert("Redeemer", decode_redeemer as DecoderFn);
        m.insert("Redeemers", decode_redeemers as DecoderFn);
        m.insert("RedeemerTag", decode_redeemer_tag as DecoderFn);
        m.insert("Relay", decode_relay as DecoderFn);
        m.insert("Relays", decode_relays as DecoderFn);
        m.insert("RewardAddress", decode_address_shim as DecoderFn);
        m.insert("RewardAddresses", decode_reward_addresses as DecoderFn);
        m.insert("ScriptAll", decode_script_all as DecoderFn);
        m.insert("ScriptAny", decode_script_any as DecoderFn);
        m.insert("ScriptDataHash", decode_script_data_hash as DecoderFn);
        m.insert("ScriptHash", decode_script_hash as DecoderFn);
        m.insert("ScriptHashes", decode_script_hashes as DecoderFn);
        m.insert("ScriptNOfK", decode_script_n_of_k as DecoderFn);
        m.insert("ScriptPubkey", decode_script_pubkey as DecoderFn);
        m.insert("ScriptRef", decode_script_ref as DecoderFn);
        m.insert("SingleHostAddr", decode_single_host_addr as DecoderFn);
        m.insert("SingleHostName", decode_single_host_name as DecoderFn);
        m.insert("StakeAndVoteDelegation", decode_stake_and_vote_delegation as DecoderFn);
        m.insert("StakeDelegation", decode_stake_delegation as DecoderFn);
        m.insert("StakeDeregistration", decode_stake_deregistration as DecoderFn);
        m.insert("StakeRegistration", decode_stake_registration as DecoderFn);
        m.insert("StakeRegistrationAndDelegation", decode_stake_registration_and_delegation as DecoderFn);
        m.insert("StakeVoteRegistrationAndDelegation", decode_stake_vote_registration_and_delegation as DecoderFn);
        m.insert("TimelockExpiry", decode_timelock_expiry as DecoderFn);
        m.insert("TimelockStart", decode_timelock_start as DecoderFn);
        m.insert("Transaction", decode_transaction_shim as DecoderFn);
        m.insert("TransactionBodies", decode_transaction_bodies as DecoderFn);
        m.insert("TransactionBody", decode_transaction_body as DecoderFn);
        m.insert("TransactionHash", decode_transaction_hash as DecoderFn);
        m.insert("TransactionInput", decode_transaction_input as DecoderFn);
        m.insert("TransactionInputs", decode_transaction_inputs as DecoderFn);
        m.insert("TransactionMetadatum", decode_transaction_metadatum as DecoderFn);
        m.insert("TransactionMetadatumLabels", decode_transaction_metadatum_labels as DecoderFn);
        m.insert("TransactionOutput", decode_transaction_output as DecoderFn);
        m.insert("TransactionOutputs", decode_transaction_outputs as DecoderFn);
        m.insert("TransactionUnspentOutput", decode_transaction_unspent_output as DecoderFn);
        m.insert("TransactionWitnessSet", decode_transaction_witness_set as DecoderFn);
        m.insert("TransactionWitnessSets", decode_transaction_witness_sets as DecoderFn);
        m.insert("TreasuryWithdrawalsAction", decode_treasury_withdrawals_action as DecoderFn);
        m.insert("UnitInterval", decode_unit_interval as DecoderFn);
        m.insert("Update", decode_update as DecoderFn);
        m.insert("UpdateCommitteeAction", decode_update_committee_action as DecoderFn);
        m.insert("URL", decode_url as DecoderFn);
        m.insert("Value", decode_value as DecoderFn);
        m.insert("VersionedBlock", decode_versioned_block as DecoderFn);
        m.insert("Vkey", decode_vkey as DecoderFn);
        m.insert("Vkeywitness", decode_vkeywitness as DecoderFn);
        m.insert("Vkeywitnesses", decode_vkeywitnesses as DecoderFn);
        m.insert("VoteDelegation", decode_vote_delegation as DecoderFn);
        m.insert("Voter", decode_voter as DecoderFn);
        m.insert("VoteRegistrationAndDelegation", decode_vote_registration_and_delegation as DecoderFn);
        m.insert("VotingProcedure", decode_voting_procedure as DecoderFn);
        m.insert("VotingProcedures", decode_voting_procedures as DecoderFn);
        m.insert("VotingProposal", decode_voting_proposal as DecoderFn);
        m.insert("VotingProposals", decode_voting_proposals as DecoderFn);
        m.insert("VRFCert", decode_vrf_cert as DecoderFn);
        m.insert("VRFKeyHash", decode_vrf_key_hash as DecoderFn);
        m.insert("VRFVKey", decode_vrfv_key as DecoderFn);
        m.insert("Withdrawals", decode_withdrawals as DecoderFn);
        m
    })
}

#[wasm_bindgen]
pub fn get_decodable_types() -> Vec<String> {
    let mut names: Vec<String> = decoders().keys().map(|k| (*k).to_string()).collect();
    names.sort();
    names
}

#[wasm_bindgen]
pub fn decode_specific_type(
    input: &str,
    type_name: &str,
    params_json: JsValue,
) -> Result<JsValue, String> {
    let params: DecodingParams = from_js_value(&params_json)?;
    let is_hex = is_valid_hex(input);
    let is_base58 = is_valid_base58(input);
    let is_bech32 = is_valid_bech32(input);

    match decoders().get(type_name) {
        Some(decoder) => decoder(input, is_hex, is_bech32, is_base58, &params),
        None => Err(format!("Unsupported type: {}", type_name)),
    }
}

#[wasm_bindgen]
pub fn get_possible_types_for_input(input: &str) -> Vec<String> {
    let params = DecodingParams::default();
    let is_hex = is_valid_hex(input);
    let is_base58 = is_valid_base58(input);
    let is_bech32 = is_valid_bech32(input);

    let mut matches: Vec<String> = decoders()
        .iter()
        .filter(|(_, decoder)| {
            decoder(input, is_hex, is_bech32, is_base58, &params).is_ok()
        })
        .map(|(name, _)| (*name).to_string())
        .collect();
    matches.sort();
    matches
}

