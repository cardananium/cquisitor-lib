{
  "name": "@cardananium/cquisitor-lib",
  "version": "0.0.1",
  "description": "A library for general-purpose utilities.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist",
    "package.json"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint .",
    "clean": "rimraf ./pkg",
    "rust:build-wasm:node": "rimraf ./pkg/node && WASM_BINDGEN_WEAKREF=1 wasm-pack build --target nodejs --out-dir pkg/node",
    "rust:build-wasm:browser": "rimraf ./pkg/browser && WASM_BINDGEN_WEAKREF=1 wasm-pack build --target browser --out-dir pkg/browser",
    "rust:build-wasm": "npm run rust:build-wasm:node && npm run rust:build-wasm:browser",
    "postbuild": "./post-build.sh",
    "build-all": "npm run clean && npm run generate-dts && npm run rust:build-wasm && npm run postbuild",
    "generate-schemas": "cargo run --bin generate_schemas schemas",
    "schemas-to-dts": "node schema-to-ts.js schemas types",
    "generate-dts": "npm run generate-schemas && npm run schemas-to-dts",
    "generate-decoders": "ts-node --esm generator.ts"
  },
  "keywords": [],
  "author": "Evgenii Lisitskii",
  "license": "MIT",
  "devDependencies": {
    "@emurgo/cardano-serialization-lib-browser": "13.2.1",
    "eslint": "^8.0.0",
    "json-schema-to-typescript": "^15.0.4",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
